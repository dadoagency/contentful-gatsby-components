import React from "react"

const ReviewBody = ({ children }) => (
  <div
    style={{
      fontFamily: "lato, arial, sans-serif",
      fontSize: "14px",
      marginTop: "1em",
    }}
  >
    {children}
  </div>
)

export default ReviewBody
